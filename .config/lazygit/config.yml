git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never
os:
  edit: "nvr -c 'lua Snacks.lazygit.open():close()' --remote {{filename}}"
  editAtLine: "nvr -c 'lua Snacks.lazygit.open():close()' --remote {{filename}} +{{line}}"
